{
  "name": "hundeschule-goller",
  "version": "1.0.0",
  "private": true,
  "description": "Website der Hundeschule Alexandra Goller (Cloudflare Worker)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "deploy": "node scripts/prepare-kv.mjs && wrangler deploy",
    "check": "node scripts/check.mjs",
    "prepare-kv": "node scripts/prepare-kv.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
